### `docs/governance-integration.md`

```md
# Governance Integration

DAOs and protocols can:

- Reference policies by `id` in on-chain governance configs.
- Require attestations that reference provider `id`s.
- Use the registry as a canonical list of approved providers.